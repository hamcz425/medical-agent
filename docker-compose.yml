version: '3.8'

services:
  medical-rag:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - rag_data:/app/data
      - rag_chroma:/app/chroma_db
    restart: unless-stopped

volumes:
  rag_data:
  rag_chroma:
